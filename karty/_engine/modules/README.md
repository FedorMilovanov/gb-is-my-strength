# karty/_engine/modules/ — LEGACY / DEAD CODE

> **Статус:** Модули созданы при попытке модульной реорганизации (2026-06-16, `9315a510`).
> Попытка сломала Авраама и потребовала 5 восстановительных коммитов (`c94a3298`–`22abf658`).
> 
> **Модули НЕ ИНТЕГРИРОВАНЫ в map-engine.js.** Движок — самодостаточный (2240 строк).
> 
> **Для новых карт:** используйте `MapEngine.createMap()`. Модули не нужны.
> **Для правки движка:** правьте `map-engine.js` напрямую.
> **НЕ импортировать эти модули в engine.js** — это сломает обратную совместимость.

## Содержимое

| Файл | Строк | Статус |
|---|---|---|
| map-data.js | 84 | Не используется |
| map-render.js | 208 | Не используется (движок имеет свой рендерер) |
| timeline.js | 54 | Не используется |
| timeline-integrated.js | 67 | Не используется (движок имеет свой timeline) |

## Если понадобится модульная архитектура

1. Сначала напишите тесты для ВСЕХ 10 карт
2. Затем рефакторите engine.js ПОСТЕПЕННО, не ломая API
3. Только после полного покрытия тестами — удаляйте этот каталог
