# Поисковый индекс — пересобирается в CI перед каждым деплоем.
# Не хранить в git: файлы меняются при каждой правке контента
# и засоряют diff, но на GitHub Pages они должны присутствовать.
pagefind/

# IndexNow key-файлы не коммитим: ключ хранится в GitHub Secrets,
# а deploy.yml генерирует <INDEXNOW_KEY>.txt только в Pages-артефакте.
34dbdd34-965b-4934-a5d4-d18a0a783600.txt
????????-????-????-????-????????????.txt
.playwright-browsers/
shots/
shots-after/
visual-audit-report.json
deep-check.json
node_modules/

# Локальные архивы репозитория не коммитим
/gb-is-my-strength.zip
audit/audit-pro-*.md

# Agent / sandbox runtime detritus — never commit
.npm/
.DS_Store
Thumbs.db
uploads/
_qa/
reports/
dist/
.astro/
.gitconfig

# Local audit tool cache
.tools/

# Local audit pasted/raw reports — use reports/local-external-checks-* instead
/LOCAL_REPO_AUDIT_REPORT.txt
/LOCAL_WINDOWS_AUDIT_REPORT.md
/summary.json

# OpenBible geocoding data local cache (atlas-enrich-places.js)
.cache/
