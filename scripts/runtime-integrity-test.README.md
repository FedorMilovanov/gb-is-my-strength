Run with Node 22:

```bash
node scripts/runtime-integrity-test.js
```

The harness is dependency-free and covers saved-quote deduplication, initial dialog accessibility state, and multi-source scroll-lock ownership.
