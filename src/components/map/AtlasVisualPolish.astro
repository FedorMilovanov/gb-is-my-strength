<style is:inline>
  /*
   * Final Atlas scenography layer.
   *
   * This component owns responsive visual hierarchy only. It never changes
   * graph data, node coordinates, filtering, focus semantics or accessibility.
   * Overview deliberately prioritises thematic hubs; focused relationships
   * always recover their full contrast through the base .is-focus contract.
   */

  .atlas-app[data-layout-profile="compact"] .atlas-stage-copy {
    display: none;
  }

  .atlas-app[data-layout-profile="compact"] .atlas-cluster-label {
    font-size: 14.5px;
    font-weight: 750;
    letter-spacing: .025em;
    opacity: .9;
  }

  .atlas-app[data-layout-profile="compact"] .atlas-node--hub .atlas-node__label {
    fill: #f0e3c7;
    font-size: 12px;
    font-weight: 750;
    stroke-width: 4.2px;
  }

  .atlas-app[data-layout-profile="compact"][data-zoom-level="overview"] .atlas-node--hub .atlas-node__halo {
    opacity: .075;
  }

  .atlas-app[data-layout-profile="compact"][data-zoom-level="overview"] .atlas-edge--cluster {
    opacity: .105;
  }

  .atlas-app[data-layout-profile="compact"][data-zoom-level="overview"] .atlas-edge--structure {
    opacity: .13;
  }

  .atlas-app[data-layout-profile="compact"][data-zoom-level="overview"] .atlas-edge--bridge {
    opacity: .075;
    stroke-dasharray: 3 7;
  }

  .atlas-app[data-layout-profile="compact"] .atlas-edge.is-focus,
  .atlas-app[data-layout-profile="compact"][data-zoom-level="cluster"] .atlas-edge--bridge,
  .atlas-app[data-layout-profile="compact"][data-zoom-level="detail"] .atlas-edge--bridge {
    opacity: .88;
  }

  @media (max-width: 680px) {
    .atlas-overview-stats {
      border-color: rgba(231, 199, 122, .1);
      background: rgba(7, 13, 21, .88);
      box-shadow: 0 18px 48px -32px #000, inset 0 1px 0 rgba(255,255,255,.025);
    }
  }
</style>
