---
/**
 * BaptistyRossiiMain.astro — <main id="main-content">...</main> of /baptisty-rossii/.
 *
 * РЕФАКТОРИНГ 5.0 Phase 6 wave 5 native pilot (AGENTS-r253). The baptisty-rossii
 * landing carries the full GBS2 chrome (mobile-head, rail, bbar, sheet) AROUND
 * <main>; extracting just <main> as semantic block keeps the page byte-identical
 * while letting the article body be promoted to a named Astro component.
 *
 * Pixel parity proof (CI):
 *   npm run visual:parity:screenshots -- --routes /baptisty-rossii/ --threshold 0.5
 */
import legacyHtml from './_legacy/main.html?raw';
---

<Fragment set:html={legacyHtml} />
