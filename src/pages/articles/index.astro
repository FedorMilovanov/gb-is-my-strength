---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { loadLegacyShadowPage } from '@/utils/legacyShadow';

// Visual-first migration: /articles/ must preserve the legacy premium catalog
// before any component refactor. The previous generic astro-card grid was a
// production-quality regression; keep this shadow wrapper until screenshot
// parity and owner review approve a hand-built Astro version.
const legacy = loadLegacyShadowPage('articles/index.html');
---

<BaseLayout
  title={legacy.title}
  description={legacy.description}
  canonical={legacy.canonical}
  robots="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1"
  ogImage={legacy.ogImage}
  ogImageAlt={legacy.ogImageAlt}
  ogTitle={legacy.ogTitle}
  ogDescription={legacy.ogDescription}
  bodyClass={legacy.bodyClass}
  mainClass="astro-main astro-main--bare"
  hideHeader={true}
  hideFooter={true}
  headHtml={legacy.headHtml}
>
  <div class="astro-articles-index-shadow" data-astro-shadow-route="articles-index">
    <Fragment set:html={legacy.bodyHtml} />
  </div>
</BaseLayout>
