---
import { getEntry, render } from 'astro:content';
import ArticleLayout from '@/layouts/ArticleLayout.astro';
import { SITE } from '@/data/site';

const entry = await getEntry('articles', 'hermenevticheskaya-otsenka-hristotsentrichnoy-germenevtiki');
if (!entry) throw new Error('Article entry not found: articles/hermenevticheskaya-otsenka-hristotsentrichnoy-germenevtiki');
const { Content } = await render(entry);
const canonical = `${SITE.url}/articles/hermenevticheskaya-otsenka-hristotsentrichnoy-germenevtiki/`;
---
<ArticleLayout
  entry={entry}
  canonical={canonical}
  robots="index, follow"
  pilot={false}
  intendedCanonical={canonical}
>
  <Content />
</ArticleLayout>
