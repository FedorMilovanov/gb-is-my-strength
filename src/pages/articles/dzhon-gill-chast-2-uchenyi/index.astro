---
import { getEntry, render } from 'astro:content';
import ArticleLayout from '@/layouts/ArticleLayout.astro';
import { SITE } from '@/data/site';

const entry = await getEntry('articles', 'dzhon-gill-chast-2-uchenyi');
if (!entry) throw new Error('Article entry not found: articles/dzhon-gill-chast-2-uchenyi');
const { Content } = await render(entry);
const canonical = `${SITE.url}/articles/dzhon-gill-chast-2-uchenyi/`;
---
<ArticleLayout
  entry={entry}
  canonical={canonical}
  robots="index, follow"
  pilot={false}
  intendedCanonical={canonical}
>
  <Content />
</ArticleLayout>
