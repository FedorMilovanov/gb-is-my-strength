---
import { getEntry, render } from 'astro:content';
import ArticleLayout from '@/layouts/ArticleLayout.astro';
import { SITE } from '@/data/site';

const entry = await getEntry('articles', '20-antisovetov-pastoru');
if (!entry) throw new Error('Article entry not found: articles/20-antisovetov-pastoru');
const { Content } = await render(entry);
const canonical = `${SITE.url}/articles/20-antisovetov-pastoru/`;
---
<ArticleLayout
  entry={entry}
  canonical={canonical}
  robots="index, follow"
  pilot={false}
  intendedCanonical={canonical}
>
  <Content />
</ArticleLayout>
