---
import { getEntry, render } from 'astro:content';
import ArticleLayout from '@/layouts/ArticleLayout.astro';
import { SITE } from '@/data/site';

const entry = await getEntry('articles', 'krajne-li-isporcheno-serdce');
if (!entry) throw new Error('Article entry not found: articles/krajne-li-isporcheno-serdce');
const { Content } = await render(entry);
const canonical = `${SITE.url}/articles/krajne-li-isporcheno-serdce/`;
---
<ArticleLayout
  entry={entry}
  canonical={canonical}
  robots="index, follow"
  pilot={false}
  intendedCanonical={canonical}
>
  <Content />
</ArticleLayout>
