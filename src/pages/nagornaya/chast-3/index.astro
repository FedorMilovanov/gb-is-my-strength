---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { loadLegacyShadowPage } from '@/utils/legacyShadow';

const legacy = loadLegacyShadowPage('nagornaya/chast-3/index.html');
---

<BaseLayout
  title={legacy.title}
  description={legacy.description}
  canonical={legacy.canonical}
  robots="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1"
  ogImage={legacy.ogImage}
  ogImageAlt={legacy.ogImageAlt}
  ogTitle={legacy.ogTitle}
  ogDescription={legacy.ogDescription}
  bodyClass={legacy.bodyClass}
  mainClass="astro-main astro-main--bare"
  hideHeader={true}
  hideFooter={true}
  headHtml={legacy.headHtml}
>
  <div class="astro-nagornaya-shadow" data-astro-shadow-route="nagornaya-chast-3">
    <div class="sr-only" data-pagefind-body>
      Кому это сказано? Часть III серии «Нагорная проповедь». Адресаты: ученики, толпа, Израиль, Церковь. Вопрос современного применения Нагорной проповеди. Диспенсационализм, заветное богословие.
    </div>
    <Fragment set:html={legacy.bodyHtml} />
  </div>
</BaseLayout>
