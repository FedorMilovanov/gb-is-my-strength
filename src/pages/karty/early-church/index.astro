---
import { loadLegacyFullDocument } from '@/utils/legacyFullDocument';

const { headHtml, bodyHtml, bodyAttributes } = loadLegacyFullDocument('karty/early-church/index.html');
---
<!DOCTYPE html>
<html lang="ru">
  <head>
    <Fragment set:html={headHtml} />
  </head>
  <body {...bodyAttributes}>
    <Fragment set:html={bodyHtml} />
  </body>
</html>
