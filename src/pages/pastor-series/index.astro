---
/**
 * /pastor-series/ — РЕФАКТОРИНГ 5.0 Phase 6 native pilot (AGENTS-r250).
 *
 * native-shadow recipe (see REFACTORING_5_0_PIXEL_DIFF_GUARD_2026-06-20.md §8).
 *
 * Pixel parity proof:
 *   npm run visual:parity:screenshots -- --routes /pastor-series/
 */
import { loadLegacyFullDocument } from '@/utils/legacyFullDocument';
import PastorSeriesMain from '@/components/pastor-series/PastorSeriesMain.astro';
import segBefore from '@/components/pastor-series/_legacy/body-segment-0.html?raw';
import segAfter from '@/components/pastor-series/_legacy/body-segment-1.html?raw';

const { headHtml, bodyAttributes } = loadLegacyFullDocument('pastor-series/index.html');
---
<!DOCTYPE html>
<html lang="ru">
  <head>
    <Fragment set:html={headHtml} />
  </head>
  <body {...bodyAttributes}><Fragment set:html={segBefore} /><PastorSeriesMain /><Fragment set:html={segAfter} /></body>
</html>
