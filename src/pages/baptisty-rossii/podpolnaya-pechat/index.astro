---
import { getEntry, render } from 'astro:content';
import ArticleLayout from '@/layouts/ArticleLayout.astro';
import { SITE } from '@/data/site';

const entry = await getEntry('articles', 'podpolnaya-pechat');
if (!entry) throw new Error('Article entry not found: articles/podpolnaya-pechat');
const { Content } = await render(entry);
const canonical = `${SITE.url}/baptisty-rossii/podpolnaya-pechat/`;
---
<ArticleLayout
  entry={entry}
  canonical={canonical}
  robots="index, follow"
  pilot={false}
  intendedCanonical={canonical}
>
  <Content />
</ArticleLayout>
