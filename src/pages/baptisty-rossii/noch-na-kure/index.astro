---
import { getEntry, render } from 'astro:content';
import ArticleLayout from '@/layouts/ArticleLayout.astro';
import { SITE } from '@/data/site';

const entry = await getEntry('articles', 'noch-na-kure');
if (!entry) throw new Error('Article entry not found: articles/noch-na-kure');
const { Content } = await render(entry);
const canonical = `${SITE.url}/baptisty-rossii/noch-na-kure/`;
---
<ArticleLayout
  entry={entry}
  canonical={canonical}
  robots="index, follow"
  pilot={false}
  intendedCanonical={canonical}
>
  <Content />
</ArticleLayout>
