# Аудит «читательского хрома» сайта (мобильная версия, книжный вид, CONTROLS, боковые оглавления, серии)

Дата: 2026-09-15 · Ветка: `arena/01a0a1ce-gb-is-my-strength` · Статус: исследование завершено, **запушено в ветку сессии** (без правок исходников сайта).
Объект: собранный `dist/` (107 HTML-страниц), исходники `src/`, `css/`, `js/`.

## 0. Метод: статика + живой Chromium

Первая фаза — статическая: DOM всех страниц, разбор CSS-каскада через postcss AST (медиа-контекст, `@layer`, специфичность, `!important`), чтение рантайм-JS.
Вторая фаза — **живой рендер**. Браузер в песочнице удалось добыть, несмотря на закрытые `cdn.playwright.dev` / `storage.googleapis.com` / `deb.debian.org`:

1. `npm i @sparticuz/chromium` (реестр npm доступен) — пакет для AWS Lambda содержит brotli-архивы `chromium.br`, `al2023.tar.br` (libnss3/libnspr4/libexpat…), `swiftshader.tar.br`, `fonts.tar.br`;
2. `chromium.br` после разброtli оказался готовым ELF-бинарём **Chromium 153.0.8010.0 (headless shell)**, а все его зависимости (13 библиотек) закрылись комплектом из `al2023.tar.br` — `ldd` чистый, ни одной «not found»;
3. бинарь поднят как CDP-сервер (`--headless=shell --remote-debugging-port=9222`, `LD_LIBRARY_PATH` на распакованные либы), Playwright 1.62.1 подключился через `connectOverCDP`;
4. `dist/` отдан статикой на `0.0.0.0:8080` (шрифты Lora/Source Sans 3 лежат локально в `dist/fonts`, внешних CDN нет → метрики текста настоящие).

Скрипты-свидетели в `reports/arena-reader-audit/` (в git попали только отчёт, скрипты и 10 скриншотов; каталог целиком в `.gitignore`, добавлено через `-f`):
статика — `inventory`, `a-assets`, `b-dom`, `c-controls`, `d-media`, `e-sel`, `f-matrix`, `g-nochrome`, `h-print`, `i-perf`, `j-width`, `k-main`, `l-ctx`, `m-dom45`, `n-geom`, `o-body`, `p-geom`, `q-tap`;
браузер — `r-geom2.mjs` (геометрия 5 страниц × 4 вьюпорта), `r-controls.mjs` (клики по настройкам), `r-mobile2.mjs` (мобильный хром/листы/стек), `r-verify.mjs` (стековый контекст, hrail-настройки), `r-line.mjs` (решающий тест интервала), `r-scan.mjs` (обход 66+8 страниц × 2–3 вьюпорта: ошибки, переполнения, обрезка); данные — `r-geom2.json`, `r-scan.json`, `r-controls.txt`; скриншоты — `shots/`.

Воспроизведение: распаковать `@sparticuz/chromium` в каталог с либами → `headless_shell --headless=shell --remote-debugging-port=9222 …` → `python3 -m http.server 8080 -d dist` → `node reports/arena-reader-audit/r-*.mjs`.

---

## 1. Карта хрома: 5 несовместимых «читалок» (статика, f-matrix)

| Семейство | Страниц | Хром |
|---|---|---|
| **Gill v16** (`.gbs-rail` + `mobile-top/bottom-bar` + `toc-overlay`×3) | **63** | 47 × `/articles/*`, 10 × `/baptisty-rossii/*`, 6 × `/hard-texts/*` |
| **hrail** (`.hrail` + `.hmtop` + `.hmbar` + `.hmsheet`) | **3** | герменевтика, код да винчи, лот и содом |
| **legacy gbs2** | **1** | `/baptisty-rossii/` — хаб серии |
| **home-navbar** | ~7 | `/hard-texts/`, `/pastor-series/` — хабы |
| **nagornaya bespoke** | 9 | `/nagornaya/*` |
| **без хрома** | ~27 | `/podrostok-za-kadrom/` (хаб серии!), `/karty/*`, `/konfessii/…`, `/about/`, `/app/`, `/map/` |

Внутри Gill v16 страницы распадаются на две разметки — это корень главных багов:

| | Семейство A (18 gill + 3 hrail = 21) | Семейство B (**45**) |
|---|---|---|
| Корень контента | `<main>` без класса → `<article class="article-body">` | `<main class="article-main">` → `<article>` без класса |
| Состав | `/articles/dzhon-gill-*` (8), все `/baptisty-rossii/*`, hrail | серия «Сердце» и примыкающие (20 URL с «сердц»), все `/hard-texts/*` (6), подростковая/взрослая серия (7), др. — список в `m-dom45.json` |

---

## 2. Баг №1 (высокий, доказан кликами): на 45 из 66 страниц читалки настройки текста мёртвы

Мост настроек единственный и требует `.article-body` (`css/reader-preferences.css:113–123`); все потребители токенов — по одному вхождению на токен во всём `dist/css`+`dist/_astro`.

**Живое доказательство** (`r-line.mjs`, 1280px, клик по каждому значению в листе настроек, замер computed-отношения line-height/font-size у абзацев тела):

| Страница | «Обычный» | «Плотный» | «Свободный» |
|---|---|---|---|
| A `/articles/dzhon-gill-chast-1-chelovek/` | 1.600 | 1.450 | 1.850 — **следует за контролом** |
| B `/articles/serdce-i-duh/` | **1.750** | **1.750** | **1.750** — застыло на дефолте `body` |

При этом на B клик честно пишет всё, кроме результата: inline `--gbs2-article-line: 1.85`, `--gb-reader-line-height: 1.85`, `localStorage['gb:reader-preferences:v1'] = {…lineHeight:"relaxed"…}` (`r-controls.txt`) — и ни один CSS-потребитель этого не подхватывает, потому что селекторы требуют несуществующий `.article-body`.

**«Ширина статьи»** на B: клик «Узкая» меняет `--gb-reader-measure` на `36rem` и inline `--gbs2-article-measure`, но медианная ширина абзаца и его координаты не меняются ни на 1280px (резерв рельса доминирует в `margin-left`), ни на мобильных, ни на ≥1600px (`--gb-measure` там игнорирует настройку). Единственный живой эффект — сдвиг колонки вправо на ~50px в окне 1380–1599px.
**«A−/A+»** работает везде: на A через CSS-токен (18.9→19.8px), на B через JS-фолбэк `floating-cluster-controller.js:1070` (`qs('article.article-body') || qs('.article-main')`), но там процент вешается на весь `<main>` → масштабируются и метаданные/подписи, не только текст.
**Персистентность** работает и даже слишком: настройки переносятся между страницами через localStorage (в одном браузере сепия/1.15/narrow из теста на B пришли на A и hrail) — это корректно, но означает, что мёртвые контролы на B молча пишут состояние, которое потом применяют страницы A.

Итог по семействам (замеры):

| Контрол | A (21) | B (45) | hrail-десктоп |
|---|---|---|---|
| Тема | ✅ | ✅ | ✅ (только солнце/луна в рельсе, см. §6) |
| Межстрочный интервал | ✅ (1.6/1.45/1.85) | ❌ мёртв всегда | ❌ лист невидим (§6) |
| Ширина статьи | ✅ на 1024–1599 и ≥1600 (дети capped) | ❌ мёртв | ❌ лист невидим |
| A−/A+ | ✅ | ⚠️ только JS, весь `<main>` | ❌ лист невидим |

---

## 3. Баг №2 (высокий, доказан скриншотом): колонка семейства B шире контейнера и обрезается правым краем

### 3.1 Механизм (уточнён живым рендером)

`site.css` (без слоя): `main.article-main{width:min(820px,92vw);margin:0 auto;padding:…24px…}`.
`floating-cluster.css:2744` (`@media(min-width:64em)`): `.gbs2-world[data-gill-v16] > .page-wrap{width:var(--gb-measure)}` = 493…760px.
Селектора, ограничивающего `main.article-main` внутри Gill-контейнера, нет (семейство A спасает `.page-wrap>main:not([class]){width:auto!important;…}`).
**Ключевая деталь, которую статика не видит:** при `width` больше содержащего блока `margin:0 auto` по спецификации схлопывается в нули → переполнение уходит **вправо**, а не симметрично (моя первоначальная модель «симметричного вылезания под рельс» неверна).

### 3.2 Замеры (`r-geom2.mjs`, `r-scan.mjs`; скриншоты `shots/B-*-1024.png`, `shots/B-*-1280.png`)

| viewport | `.page-wrap` | `main` (B) | абзац текста (B) | что видно |
|---|---|---|---|---|
| 1024 | 327→820 (493) | 351→**1171** (820) | 375→**1147** (772) | правый край вьюпорта 1024: **~123px каждой строки отрезаны**, `body.scrollWidth−clientWidth = 162`, горизонтального скролла нет (`html{overflow-x:clip}`, `body{overflow-x:hidden}`) |
| 1280 | 373→1061 (688) | 397→1217 | 421→1193 | влезает, но колонка прижата вправо: правый жёлоб 63px против рельса 335px, смещение центра **+167px** |
| 1440 | 382→1070 | 406→1226 | 430→1202 | смещение **+96px** |
| 1920 | 580→1340 (760) | 604→1424 | 628→1400 | смещение **+54px**, ширина текста фиксирована 772px (настройка игнорируется) |

Семейство A на тех же вьюпортах: `main` нейтрализован (445/640/640/712), обрезки нет; единственная асимметрия — на ≥1600px capped-дети (688px) прижаты к левому краю контейнера 712px → замер смещения **−12px** (при «Узкой» −68px).

**Масштаб по сайту** (`r-scan.mjs`, обход всех 66 страниц читалки на 1024px):
* `bodyOvfX = 162` на **45/45** страницах семейства B (и 0/3 hrail, 1/18 A — `/articles/diotrefy-nashego-vremeni/`, там своя широкая таблица);
* абзацы, уходящие за правый край вьюпорта: **38/45 B + 1/18 A = 39 страниц**;
* на скриншоте `B-serdce-i-duh-1024.png` это видно невооружённым глазом: «Бог не заводит перемену со стороны и не…», «…грех в плоти Хр…», «…страдание, раз…» — строки обрублены краем экрана, и дочитать их нельзя.

### 3.3 Мобильные (390×844, `r-mobile2.mjs`)

* «Ширина статьи» мертва на телефонах в обоих семействах: медиана абзаца 310.8px (B) / 327px (A) не меняется ни на «Узкая», ни на «Шире» (≤960px `max-width:100vw !important` перебивает measure-ограничение, а cap 576–736rem больше ширины телефона);
* «Межстрочный интервал» мёртв на B и на мобильных (32.2px = 1.75×18.4 до и после), на A работает (правило `@media(max-width:63.99em) [data-gill-v16] .article-body p`);
* ширина текстовой колонки на телефоне разная у семейств: **A 327px, B 311px, hrail 311px, nagornaya/хабы 195–301px** — четыре стандарта набора на одном сайте;
* у 45/45 B-страниц и 1/18 A на телефоне `body.scrollWidth − clientWidth = 8` — некий элемент хрома торчит на 8px за экран (скрыт clip’ом, скролла нет); на hrail и хабах — 0;
* тач-таргеты нижней панели на 390px: «Тема/Настройки/Поделиться» **34×38**, «Назад/Домой» 36×36, секция оглавления 207×37 (замер). Для сравнения палитра поиска сделана по 44px (`.cp-item{min-height:44px}`) — стандарт внутри сайта не единый.

---

## 4. Баг №3 (средний): взаимоисключающие правила в одном медиа-блоке

`floating-cluster.css:4601` `.setting-group--measure{display:none}` и `:4978` `.setting-group--measure{display:block}` — оба внутри `@media(min-width:64em)`, одинаковая специфичность → побеждает последнее. Сейчас поведение верное, но мёртвое правило-`none` — мина: любая пересборка/перенос блока спрячет контрол на десктопе. Удалить.

---

## 5. Баг №4 (средний): чужой «тёмно-золотой» `:root` внутри `@layer components`

`floating-cluster.css` содержит два блока `:root`: без слоя (строки 10–32, светлые токены от `--color-*`, `--z-sheet:2000`) и внутри `@layer components` (35–56: `--gb-accent:#d4af37`, `--gb-surface:#1a1a1a`, `--gb-text:#fff`, `--z-sheet:1000`). Unlayered всегда побеждает layered, а второй блок переопределяет ровно те же свойства → **мёртв на 100%**, но это латентный переключатель: стоит слоям перестроиться — и весь хром станет чёрно-золотым на кремовой теме. Заодно он источник путаницы `--z-sheet` 2000/1000.
Общая хрупкость: `site.css` держит базу в `@layer reset,base,components,utilities`, а `floating-cluster.css` почти весь без слоя → хром выигрывает у базы сайта независимо от порядка и специфичности; именно поэтому `main.article-main` из site.css «протёк» в Gill-контейнер (§3).

---

## 6. Баг №5 (средний, доказан): на hrail-десктопе «Настройки чтения» — невидимая кнопка

Замер (`r-verify.mjs`, `/articles/lot-i-sodom/`, 1280px): клик по `.hrail-bottom-btn[aria-label="Настройки чтения"]` переключает состояние, но лист `#hmSettings.hmsheet.hmsettings` остаётся `display:none`, сегменты «Плотный/Свободный/Узкая/Сепия» имеют rect **0×0** (`shots/hrail-desktop-settings.png`).
Причина в CSS компонента (`src/components/article-pilots/_shared/ReaderSettings.astro:99–101` + собранный `_astro/ReaderSettings.*.css`): `.hmsettings{display:none}` на верхнем уровне, а включающее правило `.hmsettings.is-open{display:block}` лежит **внутри мобильного медиа**; на ≥1200px есть только `.hmsheet:not(.hmsettings){display:none!important}` и `.hmsettings .hmsheet-panel{width:300px}` — включать нечему.
Следствие: на трёх страницах герменевтики десктопный пользователь не имеет UI для интервала/ширины/шрифта вообще (есть только переключатель темы в рельсе); мобильный (<1200px) лист при этом работает.

---

## 7. Баг №6 (средний, доказан): две шкалы ширины и две сепии

* Замер на hrail: после выбора «Узкая» в сторе `--gb-reader-measure:36rem`, а фактический inline `--hm-article-measure` = **42rem** и колонка = 672px — шкала hrail (42/50/58rem из `ReaderSettings.astro`) перебивает шкалу стора (36/43/46) на том же элементе (оба слушают `gb:reader-preferences-change`, last-writer-wins). На Gill-страницах колонка при той же настройке = 528–688px → три разных ширины текста при одном пункте меню.
* Сепия замером: Gill A/B → `rgb(238,227,200)` (`#eee3c8`, универсальный токен), hrail → `rgb(236,224,198)` (`#ece0c6 !important` из `ReaderSettings.astro:166,188`), хром Gill в сепии — отдельная палитра `#f8f0dc/#493823` (`floating-cluster.css:4504`, атрибут `data-gill-reader-theme`). Три атрибута темы (`data-reader-theme`, `data-gill-reader-theme`, `data-hm-reader-theme`) ставятся согласованно (`reader-preferences.js:150–166`), но палитры не согласованы → «Сепия» выглядит по-разному в разных статьях.

---

## 8. Баг №7 (ниже среднего, уточнён замером): z-index-эскалация не работает из-за предка

Замер (`r-mobile2.mjs`, 390px): открытый лист `.toc-overlay.is-open` имеет `z-index:2147483100`, палитра поиска `.cp-backdrop.is-open` — `2147483000`, **но рисуется поверх листа** (`elementFromPoint` в центре = `cp-item-body`, фокус = `cp-input`, лист остаётся открытым под палитрой).
Причина: лист лежит внутри `.gbs2-world{z-index:1}`, который создаёт стековый контекст → гигантский z-index листа действует только внутри этого контекста и не конкурирует с элементами корневого контекста. То есть эскалация до ~2³¹ (`floating-cluster.css:3250/3295/3519`, дубль `:4393`) бессмысленна против всего, что вне `.gbs2-world`, а сайт при этом имеет аккуратную токен-шкалу `--z-*` (до 99999), которую хром игнорирует.
Видимый симптом: при открытом листе горячая клавиша/кнопка поиска открывает палитру **поверх** листа, лист не закрывается (`search.js:ne()` не трогает `.toc-overlay`) → два модальных слоя, фокус украден; после Esc по палитре пользователь остаётся в листе. Не критично, но это ровно та «непредсказуемость слоёв», от которой спасают токены.

---

## 9. Баг №8 (низкий): дубли и вес

* Два файла одного компонента на каждой из 66 страниц: `/_astro/ReaderActionsRuntime.BcTUkum_.css` + `.C62VRiqB.css` (содержимое пересекается, но не совпадает: в одном есть блок `html[data-gb-article-tooltips-owner=…]`, в другом нет) → порядок подключения решает вид тултипов.
* Дубль правила `.mobile-bottom-bar{z-index:2147483000}` (`:3250` и `:4393`); мобильные медиа-границы 960px и 63.99em используются вперемешку → поведение окна 961–1023px определяется случайным набором правил.
* Вес (несжатые байты, `i-perf.mjs`): Gill-статья — 164KB HTML + **748KB CSS (13 таблиц)** + 346KB JS (13 скриптов); `/articles/hermenevticheskaya-otsenka…` — 388KB HTML + 653KB CSS; хаб `/baptisty-rossii/` — 735KB CSS + 536KB JS. На Gill-странице 102 `backdrop-filter`, 67 `position:fixed`. GitHub Pages сожмёт при передаче, но стоимость парсинга/композитинга на телефоне остаётся.

---

## 10. Что проверено и в порядке (не баги)

* **Настоящих JS-ошибок нет**: обход в 148 загрузок страниц (66 читалок на 1024px, 74 URL на 390px, 8 хабов на 1280px; `r-scan.mjs`) не дал ни одного `pageerror`/`console.error` от кода сайта. Все пойманные сообщения — артефакты песочницы: favicon захардкожен абсолютным `https://gospod-bog.ru/…` и на локальном хосте попадает под CSP страницы (`img-src 'self' …`, в проде это same-origin → ок), и `mc.yandex.ru/metrika` недоступен без интернета.
* **Локальных 404 нет** ни на одной странице (все ассеты, шрифты и картинки резолвятся).
* Печать: весь хром скрыт, геометрия сброшена (`@media print` в site.css/reader-preferences.css/note-registry.css) — покрытие полное (h-print).
* Нет FOUC темы: `reader-preferences-head.js` на всех 66 страницах читалки.
* DOM-гигиена: 0 дублей `id`, 0 висячих `aria-*`, 0 битых якорей (b-dom).
* Персистентность настроек между страницами и перезагрузками работает (localStorage `gb:reader-preferences:v1`), лист настроек закрывается по Esc/бэкдропу, палитра поиска держит focus-trap.
* Хабы (`/`, `/hard-texts/`, `/pastor-series/`, `/nagornaya/chast-1/`, `/izbrannoe/`, `/journal/`, `/baptisty-rossii/`) на 1280px: ни обрезки, ни переполнений, ни ошибок.
* Не подтвердился «конфликт двух бандлов»: `enhancements.js` + контроллер вместе грузятся лишь на 3 hrail-страницах, где их общих хуков (`#gbs2MobPct`, `.gbs2-sheet-toclink`) в DOM нет.

---

## 11. Приоритетный план починки (правки исходников — следующим шагом, если скажешь)

1. **Нейтрализовать `main.article-main` внутри Gill-контейнера** (чинит обрезку на 1024, прижим вправо, двойной вертикальный отступ):
   `[data-gill-v16] .page-wrap > main.article-main{width:auto!important;max-width:100%!important;margin:0!important;padding:0!important}` — аналог уже существующего правила для `main:not([class])`.
2. **Оживить настройки на 45 страницах**: расширить мост `reader-preferences.css:113–123` на main-корень (`[data-gill-v16] .page-wrap > main.article-main{font-size…;line-height…}` + cap на детей с `margin-inline:auto`) либо привести разметку B к схеме A (`<main>` без класса + `article.article-body`).
3. **Согласовать `--gb-measure` с настройкой на ≥1600px** (`min(var(--gb-reader-measure,43rem),100vw−48px)`) и центрировать capped-детей (`margin-inline:auto`).
4. **Починить hrail-десктоп**: вынести `.hmsettings.is-open{display:block}` из мобильного медиа (или добавить desktop-правило) — иначе кнопка «Настройки чтения» в рельсе остаётся пустышкой.
5. Мобильная «Ширина статьи»: либо скрыть группу ≤960px, либо завести мобильную шкалу (92%/84%/76%), которая что-то меняет на 390px.
6. Удалить мёртвое `.setting-group--measure{display:none}` (`:4601`) и мёртвый `:root` в `@layer components` (`:35–56`).
7. Одна сепия: убрать `#ece0c6 !important`, взять общий `--color-canvas`; один владелец ширины: шкала hrail → шкала стора.
8. Вернуть z-index в токены (`--z-sheet/--z-bottom-bar`), закрывать `.toc-overlay.is-open` при `gb:openSearch`; учесть, что эскалация всё равно не работает из-за `.gbs2-world{z-index:1}`.
9. Один `ReaderActionsRuntime.*.css`; унифицировать мобильные медиа-границы (960px vs 63.99em); тач-таргеты хрома до 44px (сейчас 34×38 на 390px).
10. Разобраться с 8px-переполнением на мобильных B-страницах и широкой таблицей на `/articles/diotrefy-nashego-vremeni/`.

---

## 12. Что осталось вне проверки

* Реальные устройства (safe-area на notch-телефонах, производительность 102 backdrop-filter на слабом железе), TTS/Vosk-плеер в рельсе, view-transitions между статьями разных семейств, длинные оглавления в `toc-sheet`.
* Прод-специфика: метрика, PWA/SW-режим и поведение на реальных notch-устройствах (в песочнице внешние запросы частично недоступны).
