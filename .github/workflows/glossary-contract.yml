name: Glossary Contract

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]
    paths:
      - "data/glossary*.json"
      - "css/site.css"
      - "js/glossary.js"
      - "js/site*.js"
      - "scripts/glossary-*.js"
      - "scripts/reference-system-inventory.js"
      - "scripts/tooltip-*.js"
      - "src/**"
      - "articles/**"
      - "biografii/**"
      - "hard-texts/**"
      - "konfessii/**"
      - "pastor-series/**"
      - "baptisty-rossii/**"
      - ".github/workflows/glossary-contract.yml"
  push:
    branches: [main]
    paths:
      - "data/glossary*.json"
      - "css/site.css"
      - "js/glossary.js"
      - "js/site*.js"
      - "scripts/glossary-*.js"
      - "scripts/reference-system-inventory.js"
      - "scripts/tooltip-*.js"
      - "src/**"
      - "articles/**"
      - "biografii/**"
      - "hard-texts/**"
      - "konfessii/**"
      - "pastor-series/**"
      - "baptisty-rossii/**"
      - ".github/workflows/glossary-contract.yml"

permissions:
  contents: read

concurrency:
  group: glossary-contract-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  placement-autofix:
    name: Placement and asset autofix
    if: >-
      github.event_name == 'pull_request' &&
      contains(github.event.pull_request.labels.*.name, 'autofix') &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Set up Node 22.12
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22.12.0"

      - name: Check normalizer syntax
        run: |
          node --check scripts/glossary-placement-normalizer.js
          node --check scripts/tooltip-trigger-normalizer.js
          node --check scripts/tooltip-style-normalizer.js

      - name: Test glossary placement normalizer
        run: node scripts/glossary-placement-normalizer-test.js

      - name: Test tooltip normalizers
        run: |
          node scripts/tooltip-trigger-normalizer-test.js
          node scripts/tooltip-style-normalizer-test.js

      - name: Normalize source and shared marker styles
        run: |
          node scripts/glossary-placement-normalizer.js --write
          node scripts/tooltip-trigger-normalizer.js --write
          node scripts/tooltip-style-normalizer.js --write
          node scripts/cache-bust.js --write

      - name: Validate normalized tree
        run: |
          node scripts/glossary-contract-audit.js
          node scripts/tooltip-trigger-normalizer.js
          node scripts/tooltip-style-normalizer.js
          node scripts/tooltip-icon-audit.js
          node scripts/cache-bust.js

      - name: Commit normalization
        shell: bash
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "No normalization changes required."
            exit 0
          fi
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -u
          git commit -m "fix(content): normalize glossary and tooltip contracts"
          git push origin "HEAD:${HEAD_REF}"

  source-contract:
    name: Source contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node 22.12
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22.12.0"

      - name: Prepare diagnostics
        run: mkdir -p reports

      - name: Check runtime and audit syntax
        run: |
          node --check js/glossary.js
          node --check scripts/glossary-contract-audit.js
          node --check scripts/reference-system-inventory.js
          node --check scripts/tooltip-icon-audit.js

      - name: Check normalizer syntax
        run: |
          node --check scripts/glossary-placement-normalizer.js
          node --check scripts/tooltip-trigger-normalizer.js
          node --check scripts/tooltip-style-normalizer.js

      - name: Test glossary placement normalizer
        run: node scripts/glossary-placement-normalizer-test.js

      - name: Test tooltip normalizers
        run: |
          node scripts/tooltip-trigger-normalizer-test.js
          node scripts/tooltip-style-normalizer-test.js

      - name: Audit glossary and references
        shell: bash
        run: |
          set -o pipefail
          node scripts/glossary-contract-audit.js 2>&1 | tee reports/glossary-contract.log
          node scripts/reference-system-inventory.js 2>&1 | tee reports/reference-system-inventory.log

      - name: Audit tooltip markers and styles
        shell: bash
        run: |
          set -o pipefail
          node scripts/tooltip-icon-audit.js 2>&1 | tee reports/tooltip-icon-audit.log
          node scripts/tooltip-style-normalizer.js 2>&1 | tee reports/tooltip-style.log

      - name: Lint workflow
        shell: bash
        run: |
          set -o pipefail
          node scripts/run-actionlint.mjs -no-color .github/workflows/glossary-contract.yml \
            2>&1 | tee reports/glossary-actionlint.log

      - name: Upload source diagnostics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: glossary-contract-report
          path: reports/
          if-no-files-found: warn
          retention-days: 14

  browser-contract:
    name: Browser contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node 22.12
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22.12.0"
          cache: npm

      - name: Install browser dependencies
        run: |
          npm ci
          npx playwright install --with-deps chromium

      - name: Check browser fixtures
        run: |
          node --check scripts/glossary-runtime-browser-test.js
          node --check scripts/tooltip-marker-browser-test.js

      - name: Run glossary browser fixture
        run: node scripts/glossary-runtime-browser-test.js

      - name: Run tooltip marker interactions
        shell: bash
        run: |
          set -o pipefail
          mkdir -p reports
          node scripts/tooltip-marker-browser-test.js \
            2>&1 | tee reports/tooltip-marker-browser.log

      - name: Upload browser evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tooltip-marker-browser-report
          path: |
            reports/tooltip-marker-browser.log
            reports/tooltip-marker-desktop.png
            reports/tooltip-marker-mobile.png
          if-no-files-found: warn
          retention-days: 14
