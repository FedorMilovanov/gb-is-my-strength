name: Patch WebKit overlay witness once

on:
  push:
    branches:
      - lane/system-tts-polish-2026-08-04
    paths:
      - ".github/workflows/patch-webkit-overlay-witness-once.yml"

permissions:
  contents: write

concurrency:
  group: patch-webkit-overlay-witness-once
  cancel-in-progress: false

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: lane/system-tts-polish-2026-08-04
          fetch-depth: 0

      - name: Make overlay assertions condition-based and remove this workflow
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path

          path = Path('scripts/public-surface-cross-browser-matrix.mjs')
          text = path.read_text(encoding='utf-8')

          anchor = """  }, { controls }).catch((error) => ({ found: true, controls, inspectionError: error.message }));
}

async function dismissResumePrompt(page) {"""
          helper = """  }, { controls }).catch((error) => ({ found: true, controls, inspectionError: error.message }));
}

async function waitForOverlayVisibility(page, controls, expected, timeout = 3000) {
  await page.waitForFunction(({ controls, expected }) => {
    const visible = (node) => {
      if (!node) return false;
      const rect = node.getBoundingClientRect();
      const style = getComputedStyle(node);
      return rect.width > 0 && rect.height > 0 && style.display !== 'none' &&
        style.visibility !== 'hidden' && Number.parseFloat(style.opacity || '1') > 0.01;
    };
    const target = controls ? document.getElementById(String(controls).split(/\\s+/)[0]) : null;
    const fallbacks = [...document.querySelectorAll(
      '[role="dialog"],.mobile-nav,.toc-overlay,.settings-overlay,.cp-backdrop,' +
      '.gill-settings-overlay,.mso-backdrop,.mso-panel'
    )].filter(visible);
    const node = visible(target) ? target : fallbacks.at(-1);
    return Boolean(node) === expected;
  }, { controls, expected }, { timeout }).catch(() => {});
}

async function dismissResumePrompt(page) {"""
          if text.count(anchor) != 1:
              raise SystemExit(f'Expected one helper insertion anchor, found {text.count(anchor)}')
          text = text.replace(anchor, helper, 1)

          old_open = """  await page.waitForTimeout(180);
  const state = await page.evaluate(({ controls }) => {"""
          new_open = """  await waitForOverlayVisibility(page, controls, true, 5000);
  const state = await page.evaluate(({ controls }) => {"""
          if text.count(old_open) != 1:
              raise SystemExit(f'Expected one open wait, found {text.count(old_open)}')
          text = text.replace(old_open, new_open, 1)

          old_close = """  await page.keyboard.press('Escape').catch(() => {});
  await page.waitForTimeout(180);
  let closingState = await inspectOpenOverlay(page, controls);"""
          new_close = """  await page.keyboard.press('Escape').catch(() => {});
  await waitForOverlayVisibility(page, controls, false, 3000);
  let closingState = await inspectOpenOverlay(page, controls);"""
          if text.count(old_close) != 1:
              raise SystemExit(f'Expected one close wait, found {text.count(old_close)}')
          text = text.replace(old_close, new_close, 1)

          path.write_text(text, encoding='utf-8')
          Path('.github/workflows/patch-webkit-overlay-witness-once.yml').unlink()
          PY
          node --check scripts/public-surface-cross-browser-matrix.mjs
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/public-surface-cross-browser-matrix.mjs .github/workflows/patch-webkit-overlay-witness-once.yml
          git commit -m "test(routes): await WebKit overlay state transitions"
          git push origin "HEAD:lane/system-tts-polish-2026-08-04"
